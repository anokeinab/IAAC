# Local Terraform directories and files
.terraform/
.terraform.lock.hcl
.terraform.tfstate.lock.info

# Terraform state files (DO NOT commit these)
*.tfstate
*.tfstate.*.backup
*.tfstate.backup

# Variable files that might contain sensitive data
*.tfvars
*.tfvars.json
# variables.tf

# Ignore override files as they are usually used to override resources locally
# and should not be committed
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Optional: ignore plan output files generated by `terraform plan -out=tfplan`
# *tfplan*
initializedb.sh
